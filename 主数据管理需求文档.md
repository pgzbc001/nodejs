# 主数据管理平台 业务需求文档

> 本文档描述主数据管理平台的业务需求，涵盖业务背景、目标、用户角色、功能需求及业务规则。

---

## 一、业务背景

随着企业规模不断扩大，各业务板块（采购、生产、销售、财务等）在日常运营中产生大量基础数据，如物料、供应商、客户、人员、行政区划等。这些数据分散在各业务系统中，存在以下核心痛点：

- **一物多码**：同一物料在不同系统中使用不同编码，导致采购重复、库存积压
- **数据标准不统一**：各系统字段定义、编码规则、分类体系不一致，数据无法互通
- **数据质量难以保障**：缺乏统一的质量校验机制，错误数据流入业务系统造成损失
- **变更管控缺失**：数据修改无版本记录、无审批流程，无法追溯问题根源
- **系统间协同困难**：主数据变更后无法及时同步至下游系统，造成信息滞后

为解决以上问题，需建设统一的**主数据治理平台**，实现主数据的标准化定义、集中化管控和自动化分发。

---

## 二、业务目标

| 目标 | 描述 | 衡量指标 |
|------|------|----------|
| 统一数据标准 | 建立企业级主数据模型体系，统一分类、编码、字段规范 | 主数据覆盖率 ≥ 95% |
| 消除一物多码 | 通过AI查重与编码规则，杜绝重复数据 | 重复率降至 < 1% |
| 提升数据质量 | 建立数据质量校验规则，在数据入口把关 | 数据准确率 ≥ 99% |
| 全生命周期管控 | 实现数据从创建、审批、发布、变更到废弃的全流程可追溯 | 100%操作可审计 |
| 高效数据分发 | 主数据变更后自动同步至下游系统 | 同步时效 < 5分钟 |

---

## 三、用户角色

| 角色 | 职责 | 典型操作 |
|------|------|----------|
| 数据管理员 | 负责主数据模型的定义、维护和上线管理 | 创建模型、配置字段、定义编码规则、上线模型 |
| 数据录入员 | 基于已上线模型录入和维护主数据 | 新增、修改、导入导出主数据 |
| 数据审核员 | 对数据变更进行审批和协同确认 | 审核数据、协同确认、版本回滚 |
| 系统管理员 | 负责平台整体配置和权限管理 | 分类管理、权限配置、系统监控 |
| 下游系统用户 | 消费主数据的业务系统（ERP/MES/WMS等） | 接收数据推送、反馈校验结果 |

---

## 四、功能模块一：主数据模型定义

> 本模块解决的核心问题：**"企业主数据应该长什么样"**——建立统一的数据标准规范。

### 4.1 分类体系管理

#### 业务需求

企业的主数据种类繁多，需要按照业务属性进行分层分类管理（如：行业核心经营类 → 原料/成品/半成品）。数据管理员需要能够灵活地创建、调整分类层级，形成清晰的分类树。

#### 功能要求

- 以树形结构展示分类体系，支持无限层级
- 支持新增同级分类和子级分类
- 支持对分类进行修改和删除
- 删除时需检查：该分类下是否还有子分类或已关联的数据模型，若有则不允许删除
- 支持关键字搜索快速定位分类
- 支持一键展开/折叠全部分类

### 4.2 模型创建与管理

#### 业务需求

每种主数据（如"原材料"、"供应商"、"员工"）都需要定义一个数据模型，规定该数据包含哪些信息、如何编码、有哪些约束。模型创建后需经过"上线"才能用于数据录入。

#### 功能要求

- 左侧展示分类树，右侧展示该分类下的模型列表
- 支持两种创建方式：
  - **空白创建**：从零开始定义一个全新模型
  - **继承创建**：基于已有模型快速复制创建新模型（适用于相似类型）
- 模型列表展示：模型名称、编码、所属分类、状态（已上线/未上线）、数据条数
- 模型上线后方可用于数据录入，上线后不可随意修改结构
- 支持查看模型的版本历史（版本号、操作人、操作时间、上线/下线状态）
- 支持版本回滚和版本差异对比

### 4.3 模型详情配置

#### 业务需求

模型定义是平台的核心，需要能够灵活配置模型的基础信息、字段结构、扩展特性和编码规则，以满足不同业务场景的数据管理需求。

#### 4.3.1 基础信息

定义模型的基本属性：
- **标准编码**：模型的唯一标识，不可重复
- **标准名称**：模型的中文名称
- **所属分类**：从分类树中选择归属
- **归口部门**：该模型的数据管理责任部门（如财务部、原料部、信息中心）

#### 4.3.2 字段配置

定义模型包含的数据字段及其规则，这是模型定义的核心环节。

**每个字段需配置以下业务属性：**

| 业务属性 | 说明 |
|----------|------|
| 字段名称 | 数据的业务含义（如"物料名称"、"规格型号"） |
| 显示名称 | 在界面上展示的中文标签 |
| 数据类型 | 文本、长文本、数字、日期 |
| 是否必填 | 录入时是否必须填写 |
| 是否唯一 | 该字段值是否不允许重复 |
| 是否在列表展示 | 是否在数据列表中显示该列 |
| 是否下拉选项 | 该字段是否通过下拉菜单选择（预设值域） |
| 值域来源 | 手动维护固定值列表，或引用其他模型数据 |
| 是否支持多选 | 下拉选项是否允许选择多个值 |
| 检索条件 | 是否作为列表页的搜索筛选条件 |
| 弹窗选择 | 数据量大时是否通过弹窗方式选择 |
| 是否加密 | 敏感字段是否需要加密存储和展示 |
| 数据分级 | 绝密/机密/秘密/敏感/公开 |
| 安全规则 | 关联的安全识别规则 |
| 默认值 | 新建数据时自动填充的默认值 |
| 字段分组 | 字段所属的信息分区（如"基本信息"、"技术参数"、"财务信息"） |
| 自定义校验 | 业务校验规则（如手机号格式、数值范围等） |

**引用关系配置：**
- 字段可以引用其他模型的数据作为值域（如"供应商"字段引用"供应商模型"的数据）
- 支持配置引用过滤条件（如只显示"已上线"状态的供应商）
- 支持正向级联和反向级联（选择A字段值后，B字段的可选范围自动联动）

#### 4.3.3 扩展配置

针对特殊业务场景的补充配置：
- **流程展示标题**：在审批流程中展示的标题取自哪个字段
- **树形结构**：某些模型的数据本身具有层级关系（如物料分类），需配置为树形结构
- 配置上级节点、子级节点、展示字段

#### 4.3.4 编码规则

定义主数据的自动编码生成规则，确保编码规范统一。

**编码规则类型：**

| 类型 | 说明 | 示例 |
|------|------|------|
| 固定值 | 预设的固定文本 | 如"MAT"代表物料 |
| 对象引用 | 引用其他字段的值 | 如引用"分类编码" |
| 顺序值 | 系统自动递增的数字 | 如001、002、003... |
| 编码引用 | 引用其他模型的编码 | 如引用供应商编码 |

**规则示例：** `MAT` + `分类编码(4位)` + `顺序号(6位,左补0)` → `MATYUANL000001`

**业务规则：**
- 编码全局唯一，不可重复
- "编码引用"类型只能出现在编码规则的第一段
- 顺序值由系统自动维护，按步长递增

---

## 五、功能模块二：主数据维护

> 本模块解决的核心问题：**"如何高效、准确地管理主数据"**——基于已上线的模型进行数据的全生命周期管理。

### 5.1 数据浏览与查询

#### 业务需求

数据录入员需要在已上线的模型中查看和管理主数据。左侧为分类-模型的树形导航，右侧展示选中模型的数据列表。

#### 功能要求

- 左侧树形导航按"分类 → 模型"层级展示
- 右侧数据列表根据模型定义的字段**动态展示**（不同模型的列表列不同）
- 支持按模型配置的检索条件进行筛选
- 支持数据导出为Excel
- 展示每条数据的状态（有效/停用/草稿）

### 5.2 新增主数据

#### 业务需求

录入新主数据时，系统需根据模型定义动态生成录入表单，确保数据符合预设规范，并在提交前进行查重和质量校验。

#### 功能要求

- 点击"新增"打开侧滑表单，表单根据模型字段定义动态渲染
- 字段按分组分区展示，不同类型的字段使用不同的输入控件：
  - 文本字段 → 输入框
  - 数字字段 → 数字输入框
  - 日期字段 → 日期选择器
  - 选项字段 → 下拉选择（支持多选）
  - 引用字段 → 弹窗选择 或 下拉引用
- 必填字段未填写时给出提示
- 自定义校验规则不通过时阻止提交并提示原因
- **AI查重检测**：提交时自动与已有数据比对，若发现相似数据（如名称、规格相似），弹出复核提醒
- **数据质量校验**：提交前执行质量规则检测，未通过的规则按告警等级（严重/告警/提示）展示

#### 业务规则

- 编码根据模型配置的编码规则自动生成，无需手动输入
- AI查重发现相似物料时，用户可选择"使用已有物料"或"确认是新物料，继续提交"
- 质量校验"严重"级别问题必须修正后才能提交，"告警/提示"级别可选择忽略并继续

### 5.3 修改主数据

#### 业务需求

对已有主数据进行修改时，需保留完整的修改记录，修改后需重新进行质量校验。

#### 功能要求

- 勾选一条数据后点击"修改"，打开侧滑表单并回填已有数据
- 修改提交后自动执行数据质量校验
- 每次修改自动生成新版本，保留历史快照
- 部分关键字段修改可能需要协同人确认

### 5.4 禁用/删除主数据

#### 业务需求

禁用或删除主数据前，必须确认不会影响下游业务系统（如ERP中正在使用的物料不能被禁用）。

#### 功能要求

- 勾选数据后点击"禁用"或"删除"
- 系统自动检查各下游系统（ERP、MES、WMS等）是否允许该操作
- 展示校验结果：逐行显示各系统校验状态（通过/失败/校验中）
- 全部校验通过后才允许执行操作
- 如确认影响可控，支持"忽略影响，强制操作"

### 5.5 版本管理

#### 业务需求

主数据的每次变更都需要留痕，支持回溯到任意历史版本，并在必要时回滚。

#### 功能要求

- 展示该数据的所有历史版本列表（版本号、操作类型、操作人、操作时间）
- 支持选择两个版本进行**差异对比**（逐字段展示变更内容）
- 支持**回滚到指定版本**（将历史版本数据恢复为当前版本）
- 支持查看任意版本的详细信息

### 5.6 数据导入导出

#### 业务需求

支持批量导入主数据，提升数据录入效率；支持数据导出用于线下分析或系统迁移。

#### 功能要求

- **导出**：将当前模型数据导出为Excel文件，支持按筛选条件导出
- **模板下载**：下载标准导入模板（含字段名称、格式说明）
- **批量导入**：通过Excel文件批量导入数据
- **错误处理**：导入失败的数据可下载错误明细Excel，修正后可重新导入
- 导入数据同样需经过必填校验、格式校验和质量规则校验

### 5.7 数据推送与协同

#### 业务需求

主数据创建或变更后需同步至下游系统。部分敏感数据变更需要协同人审批确认后方可推送。

#### 功能要求

- **手动推送**：选择数据后手动触发推送至指定下游系统
- **协同确认**：部分数据变更需协同人确认后才可推送
  - 系统自动判断是否需要协同
  - 选择协同人并发送通知
  - 协同人确认后数据自动推送
- **推送日志**：记录每次推送的目标系统、时间、结果

### 5.8 AI物料查重

#### 业务需求

防止"一物多码"是主数据治理的核心诉求之一。新增物料时需通过AI智能比对，识别可能的重复物料。

#### 功能要求

- 新增提交时，系统自动将关键字段（名称、规格、型号等）与已有数据进行相似度比对
- 展示相似物料列表，包含相似度百分比
- 用户可选择：
  - "使用已有物料"：放弃新增，跳转至已有数据
  - "确认是新物料"：继续提交新增

### 5.9 数据质量校验

#### 业务需求

建立数据质量管控机制，在数据入口进行质量把关，确保进入系统的主数据符合企业标准。

#### 功能要求

- 提交数据前自动执行关联的质量规则
- 质量规则类型：
  - **合规性校验**：数据是否符合编码规范、格式要求
  - **一致性校验**：数据之间是否存在逻辑矛盾
  - **完整性校验**：必填字段是否完整
- 校验结果按告警等级分类展示：
  - 🔴 **严重**：必须修正，不可忽略
  - 🟡 **告警**：建议修正，可选择忽略并说明原因
  - 🔵 **提示**：信息提醒，不影响提交

### 5.10 操作日志

#### 业务需求

所有主数据的操作都需要留痕，满足审计追溯要求。

#### 功能要求

- 记录每条数据的所有操作（新增、修改、禁用、删除、推送、协同等）
- 展示操作类型、操作人、操作时间、操作详情
- 支持按时间范围和操作类型筛选

---

## 六、业务规则汇总

### 6.1 模型管理规则

| 规则 | 说明 |
|------|------|
| 模型上线前可自由修改 | 未上线的模型可以修改字段结构、编码规则等 |
| 模型上线后结构锁定 | 已上线模型不可修改字段结构，只能新增字段 |
| 分类删除保护 | 分类下有子分类或关联模型时不允许删除 |
| 编码全局唯一 | 模型编码和主数据编码均不可重复 |

### 6.2 数据管理规则

| 规则 | 说明 |
|------|------|
| 版本自动管理 | 每次修改自动生成新版本，保留完整快照 |
| 下游依赖校验 | 禁用/删除前必须校验下游系统是否允许 |
| AI查重前置 | 新增物料时必须经过AI查重检测 |
| 质量规则前置 | 数据提交前必须通过质量规则校验 |
| 协同审批 | 部分数据变更需协同人确认后方可生效 |
| 操作全程留痕 | 所有操作记录日志，支持审计追溯 |

### 6.3 编码规则

| 规则 | 说明 |
|------|------|
| 自动编码 | 主数据编码根据模型配置的规则自动生成 |
| 编码唯一 | 系统保证同一模型下编码不重复 |
| 顺序值递增 | 顺序值段由系统维护，自动递增 |
| 补位规则 | 按配置的补位符和方向进行位数填充 |

---

## 七、非功能性需求

| 需求 | 说明 |
|------|------|
| 数据版本控制 | 所有主数据修改需保留完整历史版本，支持任意版本回滚 |
| 操作审计 | 所有操作记录日志，支持追溯 |
| 数据一致性 | 下游系统校验需保证事务一致性 |
| 编码唯一性 | 自动生成的编码必须全局唯一 |
| 动态表单 | 系统需支持动态字段定义，灵活适配不同业务模型 |
| 消息推送 | 数据变更后实时推送至订阅系统 |
| 数据安全 | 支持字段级加密、数据分级管控 |
| AI查重 | 新增数据时进行相似度检测，防止一物多码 |
| 质量管控 | 基于规则引擎进行数据质量校验 |
| 批量处理 | 支持大数据量的批量导入导出 |
| 响应性能 | 列表查询响应时间 < 3秒，表单提交响应时间 < 2秒 |
